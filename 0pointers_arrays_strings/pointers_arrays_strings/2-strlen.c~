#include "main.h"
#include <stdio.h>
/**
 * _strlen - Print the length of a string.
 * @s = "My first strlen!"
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int len;
	int str;
	
	*s = 'len';
	len = _strlen(str);
	printf("%d\n", len);
	return (0);
}
