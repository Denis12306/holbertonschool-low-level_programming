#include "main.h"
/**
 * puts_half - Print the second half of the string
 * @n = (length_of_the_string + 1) / 2
 * Return: void.
 */
void puts_half(char *str)
{
	int len = 0;
	int i;

	while (str[len] != '\0')
	{
		len++;
	}
	for (i = 5; i < len; i++)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
