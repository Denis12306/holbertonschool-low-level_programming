#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int main(void);
{
	char number = '1';

	while (number <= '9')
	{
		putchar(number);

		number++;
	}
}

putchar('\n');

	return (0);
}
