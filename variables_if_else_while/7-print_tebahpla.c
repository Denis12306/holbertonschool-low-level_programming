#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase, followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
int alphabet
{
	for (alphabet <= 'z')
	{
		putchar(alphabet);

		alphabet++;

	}
}

putchar('\n');

	return (0);
}
