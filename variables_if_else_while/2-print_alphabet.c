#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * Print the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    char alphabet = 'a';

    while(alphabet <= 'z')
    {
	    putchar("%c", a);
        alphabet++;
    }
    return (0);
}
