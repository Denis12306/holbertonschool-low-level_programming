#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Prints the numbers from 0 to 9, followed by a new line.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int main(void);
{
	int number = '0';

	while (number <= '9')
	{
		putchar(number);

		number++;
	}
}

putchar('\n');

	return (0);
}
