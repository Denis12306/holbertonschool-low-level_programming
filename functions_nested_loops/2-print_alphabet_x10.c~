#include "main.h"
#include <stdio.h>
/*
* main - Print the alphabet in lowercase 10 times.
*
* Return: Always 0 (Success)
*/
void print_alphabet(void)
{
	int i, j;
	for (i = 'a'; i <= 'z'; i++)
		for (j = '1'; j <= '10'; j++)
		{		printf ("%d\n", j);
			_putchar(i);
			_putchar(j);
		}
	}
}
