#include <stdio.h>
/**
 * argc and argv
 * Write a program that prints its name, followed by a new line.
 * Return
 */
int main(int argc, char* argv[])
{
	while (argc)
	{
		printf("Program name is: %s", argv[1]);
	}
	return (0);
}
