#include "main.h"
#include <stdio.h>
/**
 * mul - for multiplies
 * Description: Write a function that multiplies two integers.
 * @a and @b are two integers
 * n is the result of the multiplication
 * Return n
 */
int mul(int a, int b)
{
	int n;

	n = (a * b);

		return (n);
	}
