#include "main.h"
/**
 *  _isdigit - Check for a digit value
 * @c: The caracter to check
 * Description - This function check for a digit
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	if (c = '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
