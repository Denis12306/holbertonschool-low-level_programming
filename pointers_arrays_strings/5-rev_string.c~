#include "main.h"
#include <stdio.h>
/**
 * rev_string - is a function to reverse a string
 * @i is the integer
 * Return: void
 */
void rev_string(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++;
	}
	for (i = i - 1; i >= 0; i--)
	{
		_putchar(s[i]);
	}

	_putchar('\n');
}
