#include "main.h"
#include <stdio.h>
/**
 * _memcpy - a function that copies memory area
 *
 *
 *
 * Return: always 0
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	unsigned int i;

	for (i
