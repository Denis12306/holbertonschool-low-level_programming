#include "main.h"
/**
 * _puts - Write a function that prints a string, followed by a new line.
 *
 * Return: void
 */
void _puts(char *str)
{
	int i = 0;

	i = *str;

	while (i < 0)
		i++;
	_putchar(i);
}
