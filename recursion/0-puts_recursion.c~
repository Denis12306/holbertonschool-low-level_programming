#include "main.h"
#include <stdio.h>
/*
 * _puts_recursion - a fonction that print a recursion
 * @s : is the input string
 * Return: Always 0.
 */
void _puts_recursion(char *s)
{
	int i;

	if (i == 1)
		i++;
	{
		_putchar('\n');
	}
	printf(*s);
}
